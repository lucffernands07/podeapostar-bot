name: Robô de Bilhetes - SofaScore Elite

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
  schedule:
    # 11:15 UTC = 08:15 Brasília
    - cron: '15 11 * * *'
  workflow_dispatch: 

jobs:
  execucao:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar Dependências Python
        run: |
          python -m pip install --upgrade pip
          # Agora só precisamos do requests e python-dotenv
          pip install requests python-dotenv

      - name: Executar Rastreador de Bilhetes
        env:
          # --- ESSA LINHA É A QUE FALTAVA ---
          SCRAPERANT_API_KEY: ${{ secrets.SCRAPERANT_API_KEY }}
          
          # --- CREDENCIAIS DO TELEGRAM ---
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          echo "🚀 Iniciando Mineração de Elite via SofaScore..."
          python main.py
